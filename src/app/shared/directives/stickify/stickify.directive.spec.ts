import { StickifyDirective } from './stickify.directive';

describe('StickifyDirective', () => {
  it('should create an instance', () => {
    const directive = new StickifyDirective();
    expect(directive).toBeTruthy();
  });
});
